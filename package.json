{
  "name": "todo-list-full",
  "license": "MIT",
  "engines": {
    "node": "14.17.0",
    "npm": "6.14.13"
  },
  "scripts": {
    "build": "npm client install && npm client build",
    "deploy": "cp -a client/build/. public/",
    "heroku-postbuild": "npm build && npm deploy"
  }
}